import type { LucideIcon } from 'lucide-react'
import type { MoyaTone } from './types'

export type ModuleFeatureCard = {
  title: string
  desc: string
  Icon: LucideIcon
  tone: MoyaTone
  image?: string
}
